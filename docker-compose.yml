services:
  j1bots:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ./.streamlit:/app/.streamlit
      - ./chroma.db:/app/chroma.db
      - .:/app